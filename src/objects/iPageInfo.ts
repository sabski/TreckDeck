export default interface IPageInfo {
    firstPage : Boolean;
    lastPage : Boolean;
    numberOfElements : Number;
    pageNumber : Number;
    pageSize : Number;
    totalElements : Number;
    totalPages : Number;
}